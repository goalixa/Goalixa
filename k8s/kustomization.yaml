apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - secrets.yaml
  - configmap.yaml
  - pvc.yaml
  - service.yaml
  - deployment.yaml
  # - ingress.yaml  # Removed - PWA serves frontend at app.goalixa.com, backend API accessed via BFF
  - middleware.yaml
  - ingress-pwa-no-cache.yaml

images:
  - name: ghcr.io/goalixa/goalixa-app
    newName: harbor.goalixa.com/core-api/app
    newTag: 96a7bba

# Secret is managed separately in the cluster
